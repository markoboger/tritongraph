description: >-
  sbt build: 05-oss-softwaremill-tapir

  **from** = dependent module, **to** = depended-on module (`dependsOn`) or aggregated child
  (`aggregate`).

  See https://ilograph.com/docs/spec

  Source: `sbt-examples/05-oss-softwaremill-tapir/build.sbt`
resources:
  - name: verifyExamplesCompileUsingScalaCli
  - name: rawAllAggregates
  - name: rootProject
    subtitle: .
perspectives:
  - name: dependencies
    orientation: leftToRight
    color: royalblue
    notes: Relations from sbt `.dependsOn` / `.aggregate` (subset parser; not a full Scala AST).
    relations: []
