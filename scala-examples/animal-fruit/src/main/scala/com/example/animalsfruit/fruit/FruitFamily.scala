package com.example.animalsfruit.fruit

/**
 * Taxonomic-ish grouping for fruit (second enum example).
 * This is separate from `Ripeness` so the code has multiple distinct enum artefacts to parse.
 */
enum FruitFamily {
  case Citrus, Pome, Berry
}
