description: "Compose service web; image tag docker-examples-01-web:local from ./Dockerfile."
resources:
  - name: docker-examples-01-web:local
    subtitle: image
    x-triton-icon: image
    x-triton-project-compartments:
      - id: base
        title: dockerfile
        rows:
          - { value: "FROM nginx:1.27-alpine" }
  - name: web
    subtitle: service
    x-triton-icon: service
    x-triton-project-compartments:
      - id: ports
        title: ports
        rows:
          - { value: "18080:80" }
      - id: runtime
        title: container
        rows:
          - { value: "One instance per this service." }
perspectives:
  - name: dependencies
    orientation: leftToRight
    color: royalblue
    relations:
      - from: web
        to: docker-examples-01-web:local
        label: image
