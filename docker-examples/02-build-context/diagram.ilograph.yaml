description: "Service api + image docker-examples-02-api:local from Dockerfile and context ."
resources:
  - name: "docker-examples-02-api:local"
    subtitle: image
    x-triton-icon: image
    x-triton-project-compartments:
      - id: build
        title: build
        rows:
          - { label: context, value: "." }
          - { label: dockerfile, value: Dockerfile }
  - name: api
    subtitle: service
    x-triton-icon: service
    x-triton-project-compartments:
      - id: ports
        title: ports
        rows:
          - { value: "18081:8080" }
perspectives:
  - name: dependencies
    orientation: leftToRight
    color: royalblue
    relations:
      - from: api
        to: "docker-examples-02-api:local"
        label: image
